# Enable a colourful prompt
set -g default-terminal "screen-256color"

# use C-a, since it's on the home row and easier to hit than C-b
set-option -g prefix C-a
unbind-key C-a
bind-key C-a send-prefix
set -g base-index 1

# use | and - to split the windows
bind-key | split-window -h
bind-key - split-window -v

# Use Vi-style keybindings
set-window-option -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Press prefix Ctrl-hjkl to resize a pane
bind -r C-h resize-pane -L
bind -r C-j resize-pane -D
bind -r C-k resize-pane -U
bind -r C-l resize-pane -R

# When a new window is created, `cd` to the current path
bind c new-window -c "#{pane_current_path}"

# TODO When a new pane is created, `cd` to the current path
# bind c split-window -c "#{pane_current_path}" <- this doesn't work...
